{
  "name": "rap-name-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "concurrently 'nodemon server.js' 'npm run start --prefix frontend-cra'",
    "build": "npm run build --prefix frontend-cra",
    "postinstall": "npm install --prefix frontend-cra"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mongodb": "^3.6.5",
    "nodemon": "^2.0.20"
  }
}
